<%= form_with(model: ad_crawling_db, local: true) do |form| %>
  <% if ad_crawling_db.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ad_crawling_db.errors.count, "error") %> prohibited this ad_crawling_db from being saved:</h2>

      <ul>
        <% ad_crawling_db.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :ad_url %>
    <%= form.text_field :ad_url %>
  </div>

  <div class="field">
    <%= form.label :company_name %>
    <%= form.text_field :company_name %>
  </div>

  <div class="field">
    <%= form.label :img_url %>
    <%= form.text_field :img_url %>
  </div>

  <div class="field">
    <%= form.label :web_url %>
    <%= form.text_field :web_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
